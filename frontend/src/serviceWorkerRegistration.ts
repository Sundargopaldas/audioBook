// Este arquivo é baseado no template do Create React App
// Ele permite registrar um service worker para PWA (opcional)

// Se não for usar PWA, pode deixar vazio ou exportar funções vazias
export function register() {}
export function unregister() {} 